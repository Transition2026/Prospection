generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model EntrepriseExportee {
  id          Int      @id @default(autoincrement())
  siren       String   @unique
  nom         String
  exported_at DateTime @default(now())
}